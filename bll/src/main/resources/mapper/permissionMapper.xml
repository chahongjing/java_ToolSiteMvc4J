<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjy.bll.dao.PermissionDao">
	<!-- 基本操作区 -->
	<!-- 添加 -->
	<insert id="add">
		INSERT INTO Permission(<include refid="field"></include>)
		VALUES(#{permissionId, jdbcType=VARCHAR}, #{menuId, jdbcType=VARCHAR}, #{name, jdbcType=VARCHAR}, #{code, jdbcType=VARCHAR}, #{seq, jdbcType=INTEGER})
	</insert>

	<!-- 更新 -->
	<update id="update">
        update Permission
        <set>
			<if test='menuId != null and menuId != ""'>
				menuId = #{menuId, jdbcType=VARCHAR},
			</if>
			<if test='name != null and name != ""'>
				name = #{name, jdbcType=VARCHAR},
			</if>
			<if test='code != null and code != ""'>
				code = #{code, jdbcType=VARCHAR},
			</if>
			<if test='seq != null'>
				seq = #{seq, jdbcType=INTEGER},
			</if>
		</set>
         where permissionId = #{permissionId, jdbcType=VARCHAR}
	</update>

	<!-- 删除 -->
	<delete id="delete">
		DELETE FROM permission WHERE permissionId = #{id, jdbcType=VARCHAR}
	</delete>

	<!-- 获取 -->
	<select id="get" resultType="com.zjy.bll.vo.PermissionVo">
		SELECT <include refid="fieldWithPrefix"><property name="tableName" value="permission"/></include>
		  FROM permission
		 WHERE permissionId = #{id, jdbcType = VARCHAR}
	</select>

	<!-- 查询简单列表 -->
	<select id="query" resultType="com.zjy.bll.vo.PermissionVo">
		SELECT <include refid="fieldWithPrefix"><property name="tableName" value="permission"/></include>,
		        menu.name as pName
		FROM permission
		left join menu on permission.menuid = menu.menuId
		WHERE 1 = 1
		<if test='name != null and name != ""'>
			and permission.name like '%' || #{name, jdbcType=VARCHAR} || '%'
		</if>
		order by parent.seq, permission.seq
	</select>

	<!-- 查询复杂列表 -->
	<select id="queryByMapFilter" resultType="com.zjy.bll.vo.PermissionVo">
		SELECT <include refid="fieldWithPrefix"><property name="tableName" value="permission"/></include>
		FROM permission
		WHERE 2 = 2
	</select>

	<!-- 查询是否重复 -->
	<select id="queryRepeatCount" resultType="Map">
		select count(case when code = #{code, jdbcType=VARCHAR} then 1 else null end) as codeCount
		  from permission
		 where permissionId &lt;&gt; #{permissionId, jdbcType=VARCHAR}
	</select>

	<!-- 其它操作区 -->


	<!-- 公共信息 -->
	<!-- 字段 -->
	<sql id="field">
		permissionId, menuId, name, code, seq
	</sql>
	 <!-- 带前缀的字段 -->
	<sql id="fieldWithPrefix">
		${tableName}.permissionId, ${tableName}.menuId, ${tableName}.name, ${tableName}.code, ${tableName}.seq
	</sql>
</mapper>